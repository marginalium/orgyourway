steps:
  # Docker Build
  - name: 'gcr.io/cloud-builders/docker'
    args: ['build',
            '--tag=gcr.io/$PROJECT_ID/orgyourway',
            'docker/nginx'
          ]

images:
  'gcr.io/$PROJECT_ID/orgyourway'
